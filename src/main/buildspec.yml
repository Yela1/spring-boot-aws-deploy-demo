version: 0.2

phases:
  install:
    runtime-versions:
      java: 1.8
  build:
    commands:
      - ./gradlew bootJar
  post_build:
    commands:
      - echo Build completed
artifacts:
  files:
    - build/libs/*.jar
    - scripts/*.sh
    - appsepc.yml
  discard-paths: yes
